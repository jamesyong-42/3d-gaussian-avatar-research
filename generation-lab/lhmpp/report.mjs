// Produce a self-contained, offline HTML layout from retained measurements.
import fs from 'node:fs';
import path from 'node:path';
import {fileURLToPath} from 'node:url';

const file=path.resolve(process.argv[2]),dir=path.dirname(file);
const lab=path.resolve(path.dirname(fileURLToPath(import.meta.url)),'..');
const data=JSON.parse(fs.readFileSync(file,'utf8')),rows=data.results;
if(!data.pass||rows.length!==3)throw new Error('A complete passing browser comparison is required.');
const remotePath=path.join(dir,'remote-browser-results.json');
const remoteStats=fs.existsSync(remotePath)?JSON.parse(fs.readFileSync(remotePath,'utf8')).stats:null;
const remote=remoteStats?.expected===1&&remoteStats.unexpected===0?`<section class="section panel"><span class="eyebrow">Additional eight-view control check</span><h2>The receiver needs poses, not the FBX.</h2><p>The eight-photo avatar also passed recording, replay, and second-browser reproduction under injected delay, jitter, and packet loss. The receiver loaded the same portable avatar and reproduced the resolved root and joint pose without requesting the animation file. This test was separate from the performance windows above. <a href="remote-browser-results.json">Retained recording and remote-playback test</a>.</p></section>`:'';
const esc=v=>String(v??'—').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));
const rel=p=>path.relative(dir,p).replaceAll('\\','/');
const num=(n,d=1)=>Number(n).toFixed(d);
const range=(fn,d=1)=>`${num(Math.min(...rows.map(fn)),d)}–${num(Math.max(...rows.map(fn)),d)}`;
const source='https://github.com/aigc3d/LHM-plusplus/blob/906b5d9fb967ab42efb92f6fa55bf22cac86b653';
const photoDir=path.join(lab,'vendors/LHM-plusplus/assets/example_multi_images');
const inputs=rows[2].metrics.inputs.map((input,index)=>{
 const used=rows.filter(row=>row.metrics.inputs.some(i=>i.sha256===input.sha256)).map(row=>row.metrics.views+'v').join(' · ');
 return `<figure><img src="${esc(rel(path.join(photoDir,input.file)))}" alt="Bundled input view ${index}, not a held-out image" loading="lazy"><figcaption>View ${index} <span>${used}</span></figcaption></figure>`;
}).join('');
const tableRows=[
 ['Native reconstruction + PLY',r=>num(r.metrics.inferenceAndPlySeconds,2)+' s'],
 ['Cold process, including hash/load',r=>num(r.run.wallSeconds,2)+' s'],
 ['PyTorch peak allocated / reserved',r=>num(r.metrics.peakAllocatedBytes/2**30,2)+' / '+num(r.metrics.peakReservedBytes/2**30,2)+' GiB'],
 ['Raw portable binary, with fixtures',r=>num(r.metrics.assetBytes/1e6)+' MB'],
 ['Nonzero skinning influences',r=>(r.metrics.influences/1e6).toFixed(3)+' million'],
 ['Displayed browser render FPS',r=>num(r.renderFps)],
 ['Completed pose updates / second',r=>num(r.poseHz)],
 ['Worker deformation p50 / p95',r=>num(r.deformP50Ms)+' / '+num(r.deformP95Ms)+' ms'],
 ['Measured playback / samples',r=>num(r.measuredSeconds,2)+' s / '+r.samples.length],
 ['Native parity: Node + browser',r=>'7 / 7 + 7 / 7'],
 ['Mixamo body + finger mapping',r=>r.motions.find(m=>m.mapped===52).mapped+' / 52'],
].map(([label,fn])=>`<tr><th scope="row">${label}</th>${rows.map(r=>`<td>${esc(fn(r))}</td>`).join('')}</tr>`).join('');
const cards=rows.map(r=>{
 const native=['front','side','back'].map(name=>`<figure><img loading="lazy" src="${esc(rel(path.join(r.folder,'native-'+name+'.png')))}" alt="${r.metrics.views} input views, native ${name} render"><figcaption>Native ${name}</figcaption></figure>`).join('');
 const web=Object.entries(r.shots).map(([name,url])=>`<figure><img loading="lazy" src="${esc(url)}" alt="${r.metrics.views} input views, browser ${name} render"><figcaption>${name==='dance'?'Mixamo · 4.2 seconds':'Browser '+name}</figcaption></figure>`).join('');
 const stages=[['Verify checkpoint',r.metrics.hashSeconds],['Construct + load',r.metrics.loadSeconds],['Reconstruct + PLY',r.metrics.inferenceAndPlySeconds],['Portable export',r.metrics.exportSeconds]];
 const other=Math.max(0,r.run.wallSeconds-stages.reduce((a,s)=>a+s[1],0));stages.push(['Startup + renders + other',other]);
 return `<article id="v${r.metrics.views}"><header><div><span class="eyebrow">M${r.metrics.views} · nested input budget</span><h2>${r.metrics.views} photo${r.metrics.views===1?'':'s'}, one portable avatar.</h2></div><a class="button" href="${esc(r.published.url)}">Open & animate ↗</a></header><div class="gallery web">${web}</div><details open><summary>Native Gaussian-only renders · different camera from browser</summary><div class="gallery native">${native}</div></details><h3>Where the cold-process time goes</h3><div class="timing" role="img" aria-label="${esc(stages.map(([n,s])=>n+': '+num(s,2)+' seconds').join('; '))}">${stages.map(([name,seconds],i)=>`<span class="stage s${i}" style="width:${seconds/r.run.wallSeconds*100}%" title="${name}: ${num(seconds,2)} s"></span>`).join('')}</div><div class="legend">${stages.map(([name,seconds],i)=>`<span><i class="s${i}"></i>${name} <b>${num(seconds,2)}s</b></span>`).join('')}</div><details><summary>Reproducibility & validation evidence</summary><p><a href="${esc(rel(path.join(r.folder,'metrics.json')))}">Native metrics</a> · <a href="${esc(rel(path.join(r.folder,'run.json')))}">Exact container command</a> · <a href="${esc(rel(path.join(r.folder,'node-validation.json')))}">Independent Node check</a> · <a href="${esc(r.motionEvidence)}">Visible control</a> · <a href="${esc(r.backgroundEvidence)}">Avatar-hidden control</a> · <a href="${esc(rel(path.join(r.folder,'canonical.ply')))}">Canonical PLY</a></p><pre>${esc(JSON.stringify({asset:r.assetId,inputs:r.metrics.inputs,betas:r.metrics.betas,bindings:r.metrics.bindings,checkpointSha256:r.metrics.checkpointSha256,checkpointCoverageVerified:r.metrics.checkpointCoverageVerified,missingInferenceKeys:r.metrics.checkpointCoverage.missingInferenceKeys,unexpectedKeys:r.metrics.checkpointCoverage.unexpectedKeys,visiblePixels:r.visiblePixels},null,2))}</pre></details></article>`;
}).join('');
const html=`<!doctype html><html lang="en"><head><meta charset="utf-8"><meta name="viewport" content="width=device-width,initial-scale=1"><title>LHM++ · multi-photo generation, proven in the browser</title><style>
:root{font-family:Segoe UI,system-ui,sans-serif;color:#203832;background:#f3f1e9;--line:#d8dfd5;--teal:#246b58;--paper:#fffdf7}*{box-sizing:border-box}body{margin:0}main{max-width:1480px;margin:auto;padding:50px 30px}nav{display:flex;justify-content:space-between;flex-wrap:wrap;gap:15px;font-size:13px;border-bottom:1px solid var(--line);padding-bottom:18px}a{color:var(--teal);text-underline-offset:4px}h1{font:clamp(40px,5.6vw,76px)/1.02 Georgia,serif;max-width:1060px;margin:20px 0}h2{font:clamp(25px,3vw,39px)/1.15 Georgia,serif;margin:10px 0 18px}h3{font-size:17px}p,li{line-height:1.7}p{max-width:1060px}.eyebrow{font-size:12px;color:#547767;text-transform:uppercase;letter-spacing:.14em}.hero{padding:55px 0 20px}.lead{font-size:20px;max-width:990px}.pills{display:flex;gap:12px;flex-wrap:wrap;margin:25px 0}.pills span,.button{padding:10px 16px;border:1px solid var(--line);border-radius:28px;font-size:13px;background:var(--paper)}.button{background:var(--teal);color:white;text-decoration:none;white-space:nowrap}.notice{background:#eee4cc;border-left:4px solid #a57c31;padding:16px 22px;margin:28px 0}.notice p{margin:0}.flow{display:grid;grid-template-columns:repeat(5,minmax(0,1fr));gap:10px;margin:28px 0}.flow div{background:#203d34;color:#f0f5e9;padding:20px;border-radius:10px}.flow b{display:block;margin:9px 0;font-size:17px}.flow small{color:#bdcfc2;line-height:1.5;display:block}.flow em{font-size:12px;font-style:normal;color:#98c6b3}.section{margin:56px 0}.inputs{display:grid;grid-template-columns:repeat(8,minmax(0,1fr));gap:10px}figure{margin:0;min-width:0}img{display:block;width:100%;object-fit:contain;background:#e7e9e1;border-radius:8px}.inputs img{height:200px}.inputs figcaption span{display:block;color:var(--teal);font-size:11px;margin-top:4px}figcaption{font-size:12px;letter-spacing:.04em;margin-top:8px}article{margin:35px 0;background:var(--paper);border:1px solid var(--line);border-radius:17px;padding:26px}article header{display:flex;justify-content:space-between;align-items:center;gap:20px;margin-bottom:20px}article h2{margin-bottom:0}.gallery{display:grid;gap:12px}.web{grid-template-columns:repeat(4,minmax(0,1fr))}.web img{height:310px;background:#10161b}.native{grid-template-columns:repeat(3,minmax(0,1fr));max-width:850px;margin:20px auto}.native img{height:330px;background:white}details{margin:20px 0 0;font-size:13px}summary{cursor:pointer;color:var(--teal)}pre{white-space:pre-wrap;overflow-wrap:anywhere;padding:16px;background:#edf1e8;max-height:430px;overflow:auto}.table-wrap{overflow-x:auto;border:1px solid var(--line);border-radius:12px}table{width:100%;border-collapse:collapse;background:var(--paper);min-width:680px}th,td{text-align:left;padding:14px 18px;border-bottom:1px solid var(--line);font-size:14px}thead th{background:#244a3d;color:white;font-size:17px}tbody th{font-weight:500;width:39%}tbody tr:last-child>*{border:0}.timing{display:flex;height:17px;overflow:hidden;border-radius:10px;background:#e6e9df}.legend{display:flex;flex-wrap:wrap;gap:12px;margin-top:12px;font-size:11px;color:#5c6f63}.legend i{display:inline-block;width:8px;height:8px;border-radius:2px;margin-right:5px}.s0{background:#d5b878}.s1{background:#467862}.s2{background:#5facaf}.s3{background:#a184a4}.s4{background:#b8c5b3}.two{display:grid;grid-template-columns:1fr 1fr;gap:28px}.panel{border-top:2px solid #628676;padding-top:12px}.foot{font-size:12px;color:#65776b}code{background:#e9eee3;padding:2px 5px;border-radius:3px;overflow-wrap:anywhere}.sources li{margin:10px 0}footer{border-top:1px solid var(--line);padding-top:20px;margin-top:55px}section[id],article[id]{scroll-margin-top:20px}@media(max-width:850px){main{padding:25px 16px}.hero{padding-top:35px}.flow{grid-template-columns:1fr 1fr}.inputs{grid-template-columns:repeat(4,minmax(0,1fr))}.inputs img{height:180px}.web{grid-template-columns:1fr 1fr}.two{grid-template-columns:1fr}article{padding:16px}article header{display:block}article .button{display:inline-block;margin-top:16px}.web img{height:260px}.native img{height:220px}.lead{font-size:18px}}@media(max-width:450px){.flow{grid-template-columns:1fr}.inputs{grid-template-columns:1fr 1fr}.native{grid-template-columns:1fr}.native img{height:300px}.web img{height:230px}}@media print{main{padding:0}.button{display:none}.web img,.native img{height:190px}article{break-inside:avoid}.hero{padding-top:20px}}
</style></head><body><main><nav><span>GAUSSIAN AVATAR LAB / EXPERIMENT 02</span><span><a href="#results">Measurements</a> · <a href="#v1">1 photo</a> · <a href="#v4">4 photos</a> · <a href="#v8">8 photos</a> · <a href="#sources">Sources</a></span></nav><header class="hero"><span class="eyebrow">LHM++ / local RTX 4090 / executed September 8, 2026</span><h1>More observations.<br>One rig. A working web avatar.</h1><p class="lead">One, four, and eight photos now complete the LHM++ generation pipeline, export 160,000 rigged Gaussians, and animate with the existing Mixamo controls in Chromium.</p><div class="pills"><span>3 / 3 native + web trials passed</span><span>21 / 21 poses in each runtime</span><span>55 joints · 100 expression coefficients</span><span>No neural refiner in these renders</span></div></header>
<div class="notice"><p><strong>Portability proven; quality winner not established.</strong> This is one bundled same-person sequence, without calibrated held-out cameras or independently audited consent. Extra photos change the reconstruction, but these trials cannot establish identity accuracy or a general quality ranking. Unity and Quest are not tested here.</p></div>
<section><h2>The pipeline we actually ran</h2><div class="flow" aria-label="Five-stage reconstruction and animation pipeline"><div><em>01 / INPUT</em><b>Nested 1 / 4 / 8 views</b><small>Same example sequence. Native preprocessing. No camera or pose annotations supplied.</small></div><div><em>02 / RECONSTRUCT</em><b>Point–image transformer</b><small>Pinned PixelShuffle checkpoint. Native ShapeHead predicts body proportions.</small></div><div><em>03 / EXPORT</em><b>160k splats + exact bindings</b><small>Gaussian-only RGB. Every nonzero skinning influence retained. Native pose fixtures saved.</small></div><div><em>04 / CONTROL</em><b>Shared 55-joint pose</b><small>Mixamo maps 52 body/finger joints. Jaw and expression coefficients remain separate controls.</small></div><div><em>05 / DISPLAY</em><b>Browser deformation + render</b><small>Existing linear-blend worker and splat renderer. No model or GPU inference service needed for playback.</small></div></div></section>
<section class="section" id="inputs"><span class="eyebrow">Controlled input budget</span><h2>Eight observations, nested subsets.</h2><p>The one-view case is view 0, a side-facing photo—not a selected best front portrait. Four views use indices 0, 2, 4, 7; eight views use all images. Clothing and person are shared, but pose varies. These are reconstruction inputs, <strong>not held-out test photographs</strong>. ShapeHead predictions may change with the input budget; no shape value was hand-tuned.</p><div class="inputs">${inputs}</div></section>
<section class="section" id="results"><span class="eyebrow">Measured, not paper timings</span><h2>Reconstruction is quick. Exact animation is heavier.</h2><p>Native reconstruction plus PLY takes ${range(r=>r.metrics.inferenceAndPlySeconds,2)} seconds after loading. The actual fresh-container process takes ${range(r=>r.run.wallSeconds,2)} seconds, largely checkpoint verification and model construction/loading. The portable files are about ${range(r=>r.metrics.assetBytes/1e6)} MB, including seven full validation fixtures.</p><div class="table-wrap"><table><thead><tr><th>Observed measurement</th><th>1 photo</th><th>4 photos</th><th>8 photos</th></tr></thead><tbody>${tableRows}</tbody></table></div><p class="foot">One native run per setting; no latency percentiles or whole-GPU peak measurements are claimed. Memory is PyTorch allocator accounting, not all CUDA/desktop allocations. Browser: 1440 × 1000, desktop Chromium, local files, no concurrent generation. Each timing window starts after two seconds of playback warm-up, then measures ~5.5 seconds. Display FPS is the final reporting interval; pose Hz is completed worker frames across that window.</p></section>
${cards}${remote}
<section class="section two"><div class="panel"><span class="eyebrow">The important engineering difference</span><h2>The wire contract survived; the cost changed.</h2><p>The original LHM exports had 40,000 splats and roughly 61 MB binaries. LHM++ has 160,000 splats and about 4.7–4.8 million nonzero skinning influences. An unchanged pose packet can drive either rig, but evaluating that pose is much more work. Display frame rate and pose-update rate are distinct.</p><p>These families were generated with different inputs, environments, and preprocessing. This report does not declare a cross-family speed or quality winner. Keep the original 500M default while measuring LHM++ on a consented capture set and exploring a faster, parity-checked deformer.</p></div><div class="panel"><span class="eyebrow">Why the portable export is legitimate</span><h2>Bake bindings, not just a posed PLY.</h2><p>We query the native diffused skinning field at each predicted Gaussian offset, retain native fixed hand/face weights, and export neutral transforms, shape-consistent joints, and expression directions. All nonzero weights are preserved; no top-K approximation is hidden here. LHM++ rotates all splats, so the original LHM constrained-body rotation lock is not copied.</p><p>The exporter refuses assets whose expressions can move non-fixed volume queries. For these three assets, the measured expression direction outside the fixed mask is exactly zero. All seven native center/orientation fixtures pass independently in Node and the browser, before renderer packing. This is deformation fidelity, not a claim of anatomical realism or pixel-identical rendering.</p></div></section>
<section class="section two"><div class="panel"><h2>Isolation and reproducibility</h2><p>Linux CUDA runs in a dedicated Docker image and Python volume. The working Windows LHM environment and original example assets remain separate. Native runs have networking disabled, read-only checkpoints/environment, a shared GPU lease, 36 GiB host-RAM limit, and a 15 GiB C: free-space guard.</p><p>Weights and required priors are SHA-256 checked. Every reconstruction checkpoint key is required; ArcFace is the sole separately loaded module, verified by its own hash and native strict loader. The exact image ID, command, source revision, input hashes, and failures remain in run folders.</p><p><a href="${esc(rel(path.join(lab,'lhmpp/README.md')))}">Setup and reproduction notes</a> · <a href="${esc(rel(path.join(lab,'reports/lhmpp/install-20260908-170154-e2046e/environment-freeze.txt')))}">Actual environment freeze</a> · <a href="${esc(rel(path.join(lab,'reports/lhmpp/check-20260908-170932-aa0e62/environment-check.json')))}">GPU kernel preflight</a></p></div><div class="panel"><h2>What remains unproven</h2><ul><li>LHM++ is a research CLI plus published browser assets here; the web upload API still offers LHM 500M/1B, not multi-photo LHM++ jobs.</li><li>Neural refinement quality is not represented by these RGB-only splat renders.</li><li>One subject cannot establish identity fidelity, generalization, or product-use rights. No PSNR, SSIM, or LPIPS quality score was computed.</li><li>Expression dimensions are not named ARKit/Meta channels. Live device mapping and Quest performance remain future gates.</li><li>IDOL and DiGS-Avatar are still independent challenger tracks, not completed experiments.</li></ul></div></section>
<section class="section sources" id="sources"><span class="eyebrow">Research basis & pinned implementation</span><h2>Sources</h2><p>LHM++ combines point/image feature fusion with Gaussian decoding and an optional neural animation renderer. We chose its released Gaussian-capable PixelShuffle variant to test portable local playback. The paper's full-system quality results are not automatically inherited by our Gaussian-only renderer.</p><ol><li><a href="https://arxiv.org/abs/2506.13766v2">Qiu et al., LHM++: An Efficient Large Human Reconstruction Model for Pose-free Images to 3D</a> · v2, March 16, 2026.</li><li><a href="${source}/README.md">Author implementation and Gaussian-only export instructions</a> · pinned source <code>906b5d9fb967ab42efb92f6fa55bf22cac86b653</code>.</li><li><a href="https://modelscope.cn/models/Damo_XR_Lab/LHMPP-700M-PixelShuffle">PixelShuffle checkpoint</a> · revision <code>5f1c4274068e11b93721219618d36594b6087cb6</code>; 5,226,465,708-byte weight file, hash checked locally.</li><li><a href="https://huggingface.co/3DAIGC/LHMPP-Prior/tree/b683c8f68bede4f318b0bb539730b8e6711d30a0">Pinned native priors</a> · body/head models, Gaussian queries, and diffused skinning field. <a href="${esc(rel(path.join(lab,'reports/lhmpp-assets.json')))}">Local asset verification manifest</a>.</li><li><a href="${esc(rel(path.join(lab,'lhmpp/export_portable.py')))}">Our portable exporter</a> · <a href="${esc(rel(path.join(lab,'lhmpp/native_trial.py')))}">Native trial and checkpoint gates</a> · <a href="browser-results.json">All browser measurements</a>.</li></ol></section><footer><p>Generated from retained local evidence · ${esc(data.createdAt)} · no remote fonts or scripts. Input photos and avatar outputs are research examples; do not redistribute without checking upstream, body-model, and subject rights.</p></footer></main></body></html>`;
fs.writeFileSync(path.join(dir,'index.html'),html);
console.log(path.join(dir,'index.html'));
